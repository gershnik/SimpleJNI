name: CMake

on: [push]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest #${{ matrix.os }}
    # strategy:
    #   fail-fast: false
    #   matrix:
    #     os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
    - uses: actions/checkout@v2

    - name: Set up JDK
      uses: actions/setup-java@v2
      with:
        java-version: '15'
        distribution: 'adopt'

    - name: Create Build Environment
      run: cmake -E make_directory ${{runner.workspace}}/build

    - name: Configure CMake
      shell: bash
      working-directory: ${{runner.workspace}}/build
      run: cmake $GITHUB_WORKSPACE/tests -DCMAKE_BUILD_TYPE=$BUILD_TYPE 

    - name: Build and Test Native
      working-directory: ${{runner.workspace}}/build
      shell: bash
      run: cmake --build . --config $BUILD_TYPE --target run-jar

    - name: Build and Test JniGen
      working-directory: ${{runner.workspace}}/build
      shell: bash
      run: cmake --build . --config $BUILD_TYPE --target run-java-tests

    # - name: Test JniGen
    #   working-directory: ${{github.workspace}}/jnigen
    #   shell: bash
    #   run: ./gradlew -PbuildDir=build -Pcode.version=SNAPSHOT test

